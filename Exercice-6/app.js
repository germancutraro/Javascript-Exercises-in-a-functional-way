//Execute each js file with node - ejecutalo con node
  // Read two califications and return the average - Leer dos notas y devolver el promedio

//Let's do it in a functional way
const mark = (a, b) => `The final average is ${ (a + b) / 2 }`;
console.log( mark(7, 8) );

//Result = 7.5
