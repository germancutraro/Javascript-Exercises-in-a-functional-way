//Execute each js file with node - ejecutalo con node
  // Copy all elements that is in the first array in a new second array - Copiar los elementos del primer array en un segundo array

//Es6 Feature
const numbers = [1, 2, 3];
const numbers2 = [...numbers];
console.log(numbers2);

// [1, 2, 3]
